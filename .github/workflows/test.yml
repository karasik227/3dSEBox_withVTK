name: Run tests

on:
  push:
  
  workflow_dispatch:

jobs:
  tests:
    runs-on: ubuntu-latest

    steps:
      - id: Qt
        uses: jurplel/install-qt-action@v3
      - uses: actions/checkout@v1
      - name: package
        run: npm init
      - name: Run tests
        run: npm test
